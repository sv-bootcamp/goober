// import {shallow} from 'enzyme';
// import React from 'react';
import test from 'tape';
// import Index from '../../../shared/components/index';
// import TripsFixture from '../../fixtures/user.json';

test('Render the login button', (t) => {
  /*
  const wrapper = shallow(<Index onFacebookLoad={() => {
  }} user={{
    accessToken: null,
    count: -1,
    expires: -1,
    name: null
  }}/>);

  t.equal(wrapper.text().trim(), "Login with Facebook", 'login button appeared');
  */
  t.pass(true);
  t.end();
});
